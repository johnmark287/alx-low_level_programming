#include "main.h"
#include <stdio.h>
/**
 * main - Runs the program.
 * @argc: Argument count.
 * @argv: Array of string arguments.
 * Return: int.
*/
int main(int argc, char **argv)
{
    if (argc >= 0)
        ("%s\n", argv[0]);
    return (0);
}
