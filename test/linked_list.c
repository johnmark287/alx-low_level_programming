#include <sndio.h>
#include <stdlib.h>
struct node 
{
	int value;
	Node *next;
};

typedef struct node Node;

int main(void)
{
	Node *node_ptr = NULL;
	Node list;

	node_ptr = malloc(sizeof(Node));
	node_ptr->value = 0;
	node_ptr->next = NULL;

	list.

	return (0);
}
