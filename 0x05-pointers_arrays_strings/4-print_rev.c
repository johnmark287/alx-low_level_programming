#include "main.h"
#include<string.h>

/**
 * print_rev - check the code
 *
 * @s: string to reverse.
 *
 * Return: No return.
 */
void print_rev(char *s)
{
	int len, i;
	len = 0;

	while (len != '\0')
	{
		len++
	}
	if (len > 0)
	{
		for (i = len; i >= 0; i++)
		{
			_putchar(s[i]);
		}
		_putchar('\n');
	}
	else
	{
		_putchar('\n');
	}
}
