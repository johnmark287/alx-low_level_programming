#include "main.h"
#include <string.h>
/**
 * _strlen -  returns the length of a string.
 *
 * @s: string to print its length.
 *
 * Return: length of x.
 */
int _strlen(char *s)
{
	char x = strlen(s);
	return x;
}
